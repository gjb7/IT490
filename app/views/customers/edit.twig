{% extends "_layout.twig" %}

{% block title %}Edit Customer - {{ customer.name }}{% endblock %}

{% block content %}
		{% import "customers/_form.twig" as form %}
		{% from "_macros.twig" import render_flash as render_flash %}
		
		{{ render_flash(flash) }}
		
		{{ form.form(action('CustomersController@update', {'id': customer.id}), customer) }}
{% endblock %}