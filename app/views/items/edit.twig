{% extends "_layout.twig" %}

{% block title %}Edit Item - {{ item.name }}{% endblock %}

{% block content %}
		{% import "items/_form.twig" as form %}
		{% from "_macros.twig" import render_flash as render_flash %}
		
		{{ render_flash(flash) }}
		
		{{ form.form(action('ItemsController@update'), item) }}
{% endblock %}